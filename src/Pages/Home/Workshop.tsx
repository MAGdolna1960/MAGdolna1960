import { useTranslation } from "react-i18next"

const Workshop = () => {
  const { t } = useTranslation()

  return (
    <section id='workshop'>
      Workshop
    </section>
  )
}

export default Workshop