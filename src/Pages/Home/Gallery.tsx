import { useTranslation } from "react-i18next";
const Gallery = () => {
  const { t } = useTranslation()

  return (
    <section id='gallery'>
      Gallery
    </section>
  )
}

export default Gallery