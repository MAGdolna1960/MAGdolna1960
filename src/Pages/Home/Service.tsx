import { useTranslation } from "react-i18next";
const Service = () => {
  const { t } = useTranslation()

  return (
    <section id='service'>
      Service
    </section>
  )
}

export default Service